//! Append-first hook event log reader plus the Unix-socket wake-up, and
//! the settings JSON handed to Claude Code at launch. Implemented by the
//! hooks work item; see `ports::events`, spike 03, and the design.
